"""Sequential architecture: camera-independent processing + one Global Assembly.

`CAMERA = EVIDENCE, GLOBAL = MEANING.`

A second first-class execution mode beside Batch, selected with
`--mode sequential`. See ARCHITECTURE.md for the map of which components are
shared, Batch-only, or Sequential-only.
"""
