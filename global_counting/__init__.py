"""NEW global wagon counting backbone.

`runner.py` drives the external, validated `global_wagon_app` engine in an
isolated import session; `adapter.py` converts its global wagon timeline into
the unchanged Stage-1 contract the downstream pipeline already consumes.
"""
